export default function DockerPage() {
  return (
    <div>
      <h1 style={{ fontSize: "36px", fontWeight: 800, color: "white", marginBottom: "16px" }}>Docker Deployment</h1>
      <p style={{ color: "rgba(255,255,255,0.6)", fontSize: "18px" }}>Run `docker compose up` at the project root to start the proxy locally.</p>
    </div>
  )
}
